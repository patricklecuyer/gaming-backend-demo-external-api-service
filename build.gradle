plugins {
    id 'com.github.blindpirate.gogradle' version '0.7.0'
}

golang {
    packagePath = 'github.com/plecuyer/gaming-backend-demo-external-api' // go import path of project to be built, NOT local file system path!
}