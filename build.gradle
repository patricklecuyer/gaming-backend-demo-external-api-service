plugins {
    id 'com.github.blindpirate.gogradle' version '0.7.0'
}

golang {
    packagePath = 'source.developers.google.com/p/gaming-backend-demo/r/gaming-backend-demo' // go import path of project to be built, NOT local file system path!
}
dependencies {
    golang {
    }
}